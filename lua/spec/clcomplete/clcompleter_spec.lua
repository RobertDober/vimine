-- local dbg = require("debugger")
-- dbg.auto_where = 2

-- local clcompleter = require'clcomplete/clcompleter'

-- describe("nop on no match", function()
--   it("does not do much", function()
--     --             0....+....
--     local input = "ab('cd')"
--     assert.is_equal(clcompleter(input, 3), input)
--   end)
-- end)
